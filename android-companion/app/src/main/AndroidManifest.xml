<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 网络请求与状态权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 手机拍照及选择图片权限（用于小票 AA 分账拍照） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 通知读取与电量优化权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 查询已安装应用 (用于自定义监听应用列表) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLedgerCompanion">

        <!-- 主活动：全屏展示账本 UI 及离线回退视图 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 原生离线快速记账界面 -->
        <activity
            android:name=".ui.QuickAddActivity"
            android:exported="false"
            android:label="离线快速记账" />

        <!-- 监测的应用列表设置界面 -->
        <activity
            android:name=".ui.AppSelectionActivity"
            android:exported="false"
            android:label="监测的应用列表" />

        <!-- 本地通知与同步诊断日志界面 -->
        <activity
            android:name=".ui.NotificationLogActivity"
            android:exported="false"
            android:label="通知诊断日志" />

        <!-- 原生通知监听服务：系统级监听电子钱包与银行扣款通知 -->
        <service
            android:name=".NotificationService"
            android:label="@string/service_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
